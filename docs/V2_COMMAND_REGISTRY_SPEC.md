# Hotel PMS Chatbot V2 — Command Registry Specification

This file is the V2 command contract. The authoritative command set is the 19-command catalog agreed in the architecture reviews; `CONFIRM` and `CANCEL` are session actions.